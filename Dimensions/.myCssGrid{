.myCssGrid{
	display:grid;
	grid-template-columns: 1fr 300px 20%;
	grid-template-rows: repeat(3, 300px);
	grid-gap: 2em;
 	 
}
.myNewCSSGrid{
	gridd-template-columns: repeat(auto-fill, 230px); //as wide as viewport, not using minmax
}
.myNewCSSGridMinMax{
	gridd-template-columns: repeat(auto-fill, minmax(230px, 1fr) ); //as wide as viewport, not using minmax
}

.CHILD
{
	grid-area: CHILD;
	background-color:crimson;
}